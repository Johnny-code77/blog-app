// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("mongodb+srv://matoloanemolapo7:vEEWC5gZMNA0iNev@johnny.m8qb3.mongodb.net/contacts_db?retryWrites=true&w=majority")
}

model Contacts {
  id        String  @id @default(auto()) @map("_id") @db.ObjectId
  fullName  String
  email     String  @unique
  company   String
  message   String
  createdAt DateTime @default(now()) @map("created_at")
}
